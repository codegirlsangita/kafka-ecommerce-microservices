global:
  scrape_interval: 5s

scrape_configs:
  - job_name: order-service
    metrics_path: /metrics
    static_configs:
      # Services run on the host (npm run dev), not in Docker — host.docker.internal
      # lets the Prometheus container reach back out to the host machine.
      - targets: ['host.docker.internal:3002']
